[project]
name = "autoresearch"
version = "0.1.0"
description = "Autonomous fine-tuning research pipeline (Qwen3.5-0.8B SFT / LoRA)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    # Model training
    "torch>=2.1.0",
    "transformers>=5.5.0",
    "accelerate>=1.13.0",
    "safetensors>=0.7.0",
    "peft>=0.16.0",
    # Pipeline utilities
    "pyyaml>=6.0",
    "matplotlib>=3.8.0",
    "requests>=2.32.0",
]
